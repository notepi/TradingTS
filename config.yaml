# TradingAgents 项目配置
# 此文件提交到 GitHub，包含非敏感配置

# 分析目标
stock_list:
  - "688333.SH"

# 模型配置
llm:
  provider: dashscope
  model: glm-5
  vision_model: qwen-vl-max-latest
  translation_model: qwen3-max-2026-01-23

# 输出配置
output:
  language: Chinese
  log_level: INFO